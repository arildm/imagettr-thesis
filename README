Implementing perceptual semantics in Type Theory with Records (TTR)

A master thesis in Language Technology
By Arild Matsson <arild@klavaro.se>


The Latex document needs the fonts Free and Liberation.

To use `latexmk` to compile and view the document, first put this in `~/.latexmkrc`:

    $pdflatex = q/xelatex -synctex=1 %O %S/;

Then execute

    latexmk -pdf imagettr.tex

or

    latexmk -pdf -pvc imagettr.tex
